#
# Build Deploy file for Docker container
# Change the variables in this section to match your project
#

name: Bug Report Web - Deploy
permissions:
  contents: read

defaults:
  run:
    working-directory: .
    
#
# Everything below this is template code
#

on:
  release:
    types: [published]

  pull_request:
    types: [closed]
    branches: ["master"]

  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production

jobs:
  staging:
    if: github.event.pull_request.merged == true || github.event.inputs.environment == 'staging' || github.event.inputs.environment == 'production'
    name: Deploy to Staging
    uses: ./.github/workflows/internal-deploy-step.yml
    with:
      environment: staging
      docker_path: "BugReportWeb"
      config_file: "BugReportWeb/Config/appsettings.Production.json"
      project_name: "BugReportWeb"
      external_port: 8005
      server_external_port: 8004
    secrets: inherit

  production:
    if: github.event.pull_request.merged == true || github.event.inputs.environment == 'production' || github.event_name == 'release'
    name: Deploy to Production
    uses: ./.github/workflows/internal-deploy-step.yml
    with:
      environment: production
      docker_path: "BugReportWeb"
      config_file: "BugReportWeb/Config/appsettings.Production.json"
      project_name: "BugReportWeb"
      external_port: 9005
      server_external_port: 9004
    secrets: inherit
    needs: staging
